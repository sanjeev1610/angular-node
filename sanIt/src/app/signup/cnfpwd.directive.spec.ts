import { CnfpwdDirective } from './cnfpwd.directive';

describe('CnfpwdDirective', () => {
  it('should create an instance', () => {
    const directive = new CnfpwdDirective();
    expect(directive).toBeTruthy();
  });
});
